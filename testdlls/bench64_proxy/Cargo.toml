[package]
name = "bench64_proxy"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
dllproxymacros = "0.2.0"
winapi = { version = "0.3.9", features = ["libloaderapi", "minwindef"] }

[workspace]